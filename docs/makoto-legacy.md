# 旧 MAKOTO の経緯と引き継ぐ資産

作り直し（makoto2）の前提となる、旧実装 [pooza/makoto](https://github.com/pooza/makoto) の経緯・停止理由・残っているデータ。**コードは引き継がないが、台詞コーパスは引き継ぐ価値がある。**

## 経緯

- 旧実装は長期間メンテされていなかった（最終コミット 2024-11-10）
- **2026-06-24 に全停止**（`makoto_enable=NO`）。同居していた本番サーバー（lbock）で**孤児プロセスが 11 日分たまり swap を約 800MB 占有**しており、キュアスタ！の投稿レイテンシ劣化の一因になっていた
- 以後ずっと停止したまま。アカウント [@makoto](https://precure.ml/@makoto) は 44,161 投稿・最終投稿 2026-06-24 で凍結状態
- **2026-07-29 夜に lbock が DNS ごと消えた**（サーバー移行で退役）。lbock 上にしか無かった実行時設定（`.env` 等）はもう取れない
- 旧リポジトリは 2026-07 にアーカイブ済み

## データはバックアップサーバーにだけ残っている

lbock 本体は消えたが、社内バックアップサーバー上の rsync バックアップが生きている。

| ファイル | 中身 |
| --- | --- |
| `makoto_2026-02-13.sql.zst`（39KB） | **唯一の DB スナップショット** |
| 旧 `local.yaml`（2024-11-11） | 旧実装の実行時設定 |

**具体的な所在・取り出し手順は [pooza/chubo2](https://github.com/pooza/chubo2)（private）側を参照する**（このリポジトリは public のため書かない）。ファイル所有者・パーミッションの都合でバックアップサーバー上での昇格が必要。

⚠ **スナップショットは 2026-02-13 で止まっている**（停止した 06-24 の 4 ヶ月前）。lbock は writersbase-tools を使っておらず Google Drive バックアップにも含まれていないため、**それ以降の変更は取得できない**。

### 取り出し済み（2026-07-30）

ダンプはローカルの `var/corpus/` に取得済みで、[tools/pgdump_to_json.py](../tools/pgdump_to_json.py) でテーブルごとの JSON に落としてある。**`psql` を必要とせず、COPY ブロックを直接読む**ので、データストアが決まる前でも扱える。

```sh
python3 tools/pgdump_to_json.py var/corpus/makoto_2026-02-13.sql var/corpus
```

⚠ **`var/` はコミットしない**（`.gitignore` 済み）。このリポジトリは public で、コーパスには**第三者の著作物である台詞**と、`account` 393 件の**アカウント情報**が含まれる。取り出し元の所在と手順は `pooza/chubo2`（private）側を参照。

### スナップショットの中身（2026-02-13 時点の行数）

| テーブル | 行数 |
| --- | --- |
| `quote` | **956** |
| `account` | 393 |
| `message` | **388** |
| `fairy` | 62 |
| `series` | 7 |
| `form` | 3 |
| `keyword` / `past_keyword` / `track` | **0** |

`track`（曲）が 0 件なのは「曲管理」の Issue が未実装だったことと整合する。**歌う中身のデータは残っていない**ので、曲は新規に集める（→ [track-corpus.md](track-corpus.md)）。

## 引き継ぐ資産: 台詞コーパス（quote + message ≒ 1,340 行）

**人手のメタ情報が付いており、そのまま新機能に対応する。** ここが作り直しでも引き継ぐ価値のある唯一の資産。

### quote（956 件）

スキーマは `quote(series_id, form_id, episode, emotion, exclude, exclude_respond, priority, body, remark)`。**「応答に使ってよいか」「優先度」「変身前後どちらの口調か」が既に人手で付いている。**

| 区分 | 件数 |
| --- | --- |
| **応答に使える台詞**（`exclude=f` かつ `exclude_respond=f`） | **561**（剣崎真琴 395 / キュアソード 165） |
| 投稿だけに使う（応答不可） | 289 |
| 完全に封印 | 106 |
| 話数（`episode`）が入っているもの | 871 |

- `form` = キュアソード / 剣崎真琴 / ちびキュアソード ＝ **変身前後で口調が分けてある**＝ペルソナ切替に直結する。応答可 561 件の内訳は剣崎真琴 395 / キュアソード 165 / **ちびキュアソード 1**
- 出典は `series` 7 作（ドキドキ!プリキュアが 870 件で主）
- `priority` は 1〜5 に分布する（既定の 3 が 475 件、1 が 167 / 2 が 158 / 4 が 139 / 5 が 17）。**抽選の重みとしてそのまま使える**
- `emotion` は 956 件中 25 件が `bad` で残りは null。**未使用ではなく、旧実装の好感度モデルの一部**（下記）

### message（388 件）

`type` が**絞り込んだ 4 機能とほぼ一対一に対応している**。

| type | 件数 | 対応する機能 | 原稿として使えるか（2026-08-05・#12 で実測） |
| --- | --- | --- | --- |
| `morning` | **237** | 朝挨拶 | ✅ 使う |
| `template` | 109 | 汎用テンプレート | ❌ **全 109 件が `%s` を含む** |
| `birthday` | **23** | バースデーライブ | 🔴 **落とした**（2026-08-27・#60。⚠ **用途をライブの台本が吸収した**・下記） |
| `calling` | 13 | 呼びかけ | ❌ **全 13 件が `%s` を含む** |
| `holiday` | 6 | 祝日 | ✅ 使う |

**`message` は `month` / `day` 列を持つ**＝「特定の日は前もって用意した原稿で上書きする」は旧実装で既に設計済み。

#### ⚠⚠ `template` と `calling` の 122 件は原稿ではない（#12 で実測）

**両方とも全件が `%s` を含む穴埋めテンプレート。**⚠ **そのまま投稿すると `%s` が出る。**`template` は `feature`（`一般` 59 / `人名` 39 / `地域` 11）と対応しており、未実装だった**キーワード学習**の素材。`calling` は相手の名前を差し込む**呼びかけ**で、チャットボット（#18）が使うならそちら。

⚠ **原稿の上書き機構（#12）の許可リストに入れてはいけない。**逆に `morning` / `birthday` / `holiday` の 266 件には `%s` が 1 件も無い。

#### 🔴 `birthday` 23 件は落とした（2026-08-27・#60）

⚠⚠ **用途をライブの台本が吸収した。**11/4 は 8 時間のバースデーライブ当日で、**`live_open` / `live_mc` / `live_close` が枠を全部埋める**（→ [birthday-live.md](birthday-live.md)）。⚠ **旧 `birthday` は進行を持たない静的な宣言**（「届くかな、わたしの歌。」「わたしは歌うプリキュアよ！」）なので、🔴 **8 時間のどこに置いても同じものになる** — **MC 台本（#13）が「1曲目 → ここで半分 → 6時間 → 最後の1曲」と進行を持っているのと対照的。**

##### ⚠⚠ 実測: 23 件のうち固有なのは 6 件だけ

⚠ **`tools/morning_profile.py` が出す**（`difflib` の比率 0.8 以上を「ほぼ同一」とする）。

| | 件数 | 中身 |
| --- | --- | --- |
| 🔴 **`morning` と本文が完全一致** | **12** | ギターの練習・きのこが嫌い・バーベキュー・きゅうり・流れ星・星が綺麗・生姜焼き・親切な人・**季節指定を持つ 4 件すべて**（紅葉 ×2 / 葡萄 / 梨） |
| ⚠ **`morning` とほぼ同一**（句読点・語尾だけ違う） | **5** | 歌は元気の源・歌を通しての出会い・いちご / イチゴ屋・焼肉 / トントロ・プリン |
| ✅ **固有** | **6** | 🔴 **すべて「歌を語る」宣言**（「わたしは歌うことが大好きです」「届くかな、わたしの歌。」「わたしは歌うプリキュアよ！」ほか） |

⚠ **季節指定（`{"season": [9,10]}` / `[8,9]`）を持つ 4 件は、中身が紅葉・葡萄・梨のダジャレ**＝秋の朝挨拶で、**そのまま `morning` にも入っている。**🔴 **11/4 に出すと季節外れになる形だったが、落としたので考えなくてよくなった。**

##### 落とし方

| | 変更 |
| --- | --- |
| **取り込み** | `CorpusImporter::SKIP_MESSAGE_TYPES`（⚠ **`account` / `fairy` を取り込まないのと同じ扱い**）。🔴 **正本は `MessageRepository::DROPPED_TYPES`** |
| 🔴 **書ける口** | ⚠⚠ **`MessageRepository#create` / `#upsert` が `ValidateError` で弾く。**⚠ **消す側だけを持つと「`makoto message add --type birthday` で足せるのに、次の投入で黙って消える」**（Codex の指摘・PR #207）。⚠ `makoto message preview` の既定 type からも外した |
| 🔴 **既に入っている行** | ⚠⚠ **投入のたびに消す**（`purge_messages`）。**取り込まないだけでは残る** — 🔴 **`bydo` の DB には 23 件が入っていた**（2026-08-27 実測）。⚠ **`makoto corpus import` を流せば消える**ので、手で消して回る手順を作らない |
| **記念日の登録** | ⚠⚠ **`/message/anniversary` の `11-04` から `birthday` を外した** |
| **許可リスト** | 朝挨拶（#17）は `holiday` / `morning` の 2 つ（⚠ **`birthday` を入れない**） |

⚠ **`template` 109 件 / `calling` 13 件は落としていない。**あちらは原稿ではないが、**キーワード学習・呼びかけ（#18）の素材として残す**（上記）。

⚠ **「誕生日らしい原稿」を確実に出したいなら、11/4 用の原稿を年指定（`year`）で書き下ろす**（→ #13。**段 1 なので記念日より優先される**）。

#### 日付の指定は 2 系統ある（実データで確認）

⚠ **`month` / `day` を使っているのは `holiday` の 6 件だけ。`birthday` 23 件は日付を持たない。**「11/4 に出す」判定は日付列ではなく **`type` の側**で行われていた。原稿の上書き機構（#12）を作るときの前提になる。

`month`/`day` を持つ 6 件は元日・プリキュアの日（2/1）・バレンタイン・エイプリルフール・**キュアスタ！の誕生日（4/23）**・クリスマスイブ。

もう 1 系統が **`data` 列（json）の `season`** で、**月の配列**を持つ。

| type | `data` あり | 例 |
| --- | --- | --- |
| `morning` | 82 / 237 | `{"season": [6,7,8]}` |
| ~~`birthday`~~ | ~~4 / 23~~ | ⚠ **落とした**（上記） |

つまり旧実装の原稿選択は「**特定日（month/day）→ 季節（data.season）→ 無指定**」の 3 段構えだった。makoto2 でも**季節フィルタは引き継ぐ価値がある**（朝挨拶 237 件を通年で回すと季節外れが出る）。

✅ **makoto2 では「その年のその日 → 毎年のその日 → 記念日 → 季節 → 無指定」の 5 段にした**（#12・`MessageSelector`）。⚠ **年（`year` 列）と記念日（`/message/anniversary`）が新しく足した段**で、前者は台本（#13 / #14）、⚠⚠ **後者は当初「日付を持たない `birthday`」のためだったが、その `birthday` を落とした**（上記）ので、🔴 **いま段 3 を使うのは予告（`announcement`）とライブの台本だけ。**

### 🔴 朝挨拶原稿の仕様（`morning` 237 件・2026-08-27 実測・#60）

**総入れ替えすると二度と測れない**ので、入れ替える前に「何が『らしさ』を作っているのか」を数字で残す。⚠ **#17（朝挨拶）はこの節を満たすものを作る。**

🔴 **測り方の正本は [tools/morning_profile.py](../tools/morning_profile.py)。**⚠⚠ **数字だけを置かない** — **規則を書き残さないと、次に測った人が違う規則で違う数字を出して、どちらが正しいのか分からなくなる**（実際に下記の register でそうなっている）。

```sh
python3 tools/morning_profile.py var/corpus
```

#### 形

| | 実測 |
| --- | --- |
| **文字数** | ⚠ **平均 41.8 字**（中央 38・最短 9・最長 122） |
| **文の数** | 平均 2.2 文（中央 2・最多 6） |
| 🔴 **行数** | **2 行 58.6%**（139 件）／1 行 29.5%（70 件）／3 行 11.0%（26 件）／4 行 0.8%（2 件） |

⚠⚠ **既定は「2 行」。**⚠ **1 行目で出来事を言い、2 行目で感想か落ちを付ける**という形が過半数を占める。**1 行だけのものは短い一言**（「今日も一日頑張りましょう。」）で、**3 行以上は 12% しかない。**

#### register — 🔴 **敬体が基底**

⚠ **規則**（`morning_profile.py`）: 文末から飾り（`。！？♪☆` と絵文字）を落とし、**`です` / `ます` の語幹 ＋ 終助詞**なら敬体、**`だわ` / `のよ` / `かな` / `よ` / `ね` 系**なら常体、それ以外（体言止め・命令形・動詞の終止形）は「どちらでもない」。

| | 朝挨拶 `morning` 237 件 | 本編 `quote` 956 件 | 本編 応答可 561 件 |
| --- | --- | --- | --- |
| **文単位・敬体** | 🔴 **63.9%** | **5.7%** | 7.1% |
| 文単位・常体 | 3.8% | 20.1% | 22.1% |
| 文単位・どちらでもない | 32.3% | 74.2% | 70.8% |
| **締めの文が敬体** | **63.7%** | 6.0% | 7.7% |
| **1 文でも敬体を含む** | ⚠ **87.8%** | 6.8% | 8.0% |

🔴 **朝挨拶の敬体率は本編の 11 倍。**⚠⚠ **これは揺れではなく宛先による使い分け**（→ [makoto-persona.md](makoto-persona.md)「口調は『一致』ではなく register で見る」）。**本編は仲間に向けた言葉で、朝挨拶は視聴者・ファンに向けた言葉。**

##### ⚠⚠ 旧計測の「敬体 41.8%」は再現できない

⚠ [makoto-persona.md](makoto-persona.md) は `morning` の敬体を **41.8%**（＝ 99 / 237）と書いているが、🔴 **その規則が残っておらず、どう数えても 99 にならない**（2026-08-27 に 7 通りの規則で試した。近いのは「原稿末が `です` / `ます` 系 ＝ 144」「原稿末が `です。` / `ます。` ＝ 59」）。

⚠⚠ **結論のほうは変わらない** — **「本編より一桁多い」「宛先で使い分けている」はどの規則で測っても成り立つ**（10 倍 → 11 倍）。⚠ **数字だけを差し替えず、両方を残す**（旧計測の規則が後から出てきたときに突き合わせられるように）。

⚠ **同じ文書の「呼びかけ みなさん 13 / みんな 8」「文末 です・ます 99 / だ・だよ・かな 35」のうち、呼びかけの 13 / 8 はこちらの実測と一致する。**🔴 **ずれているのは文末の数え方だけ。**

#### 一人称は 5 回に 1 回しか出さない

| 語 | 件数 |
| --- | --- |
| **真琴（自称）** | **30** |
| うち 🔴 **「〜な真琴です / でした」で閉じる** | ⚠ **16** |
| わたし | 14 |
| 私 | 4 |
| みなさん・皆さん | 13 |
| みんな | 8 |

⚠⚠ **48 件（20%）にしか一人称が出ない。**⚠ **日本語なので主語を落とすのが既定**で、**出すときは自称「真琴」のほうが「わたし」より多い。**

🔴 **「〜な真琴です」は署名オチという 1 つの型**（16 件・6.8%）。⚠ **「ボウリングはあんまり得意ではない真琴です。」のように、自分を三人称で呼んで落ちにする。**⚠⚠ **ダジャレと並ぶ構成要素なので、#17 で落とすと目に見えて痩せる。**

#### 話題の分布 — ⚠⚠ **食べ物と天気で 3 分の 2**

⚠ **多重ラベル**（1 件が食べ物と季節の両方に当たる）。237 件中 230 件がどれかに当たる。

| 話題 | 件数 | 割合 |
| --- | --- | --- |
| 🔴 **食べ物・飲み物** | **91** | **38.4%** |
| 🔴 **天気・季節** | **80** | **33.8%** |
| **歌・仕事** | 54 | 22.8% |
| 星・月・自然 | 26 | 11.0% |
| 髪・服・おしゃれ | 22 | 9.3% |
| 感謝・励まし | 21 | 8.9% |
| 買い物・持ち物 | 20 | 8.4% |
| 遊び・スポーツ | 16 | 6.8% |
| 学校 | 9 | 3.8% |
| 動物 | 8 | 3.4% |
| 映画・観劇・鑑賞 | 8 | 3.4% |

⚠ **どれにも当たらない 7 件は「日常の気づき・失敗談・標語」**（「階段ってすごい」「ロッカーは硬い」「一人はみんなのために」「ファイトだ、オー！」）。

🔴 **この偏りが「らしさ」の実体。**⚠⚠ **話題を均等に散らすと別人になる** — **食べ物を 4 割・天気を 3 割にするのが仕様。**

##### 🔴 上の数字は「旧 237 件の語彙で作ったリスト」で測ったもの（2026-09-05 に直した・#225）

⚠⚠ **`TOPICS` のキーワードは旧 237 件を読んで作った**ので、🔴 **新しく書いた原稿の語彙を知らない。**⚠ **「新玉ねぎ」「オムライス」「お味噌汁」「唐揚げ」「たけのこ」はどれも食べ物だが、1 語も当たらず「どれにも当たらなかった」に落ちていた。**

⚠ **実測（2026-09-05）**: 🔴 **`makoto-scripts` の 325 件のうち 39 件が未分類で、そのうち 33 件が #225 で足したもの。**⚠⚠ **話題の配分が実際より低く出ていた**（**食べ物 32.9% と読めていたが、語彙を足すと 36.9%**）。

🔴 **原稿を足すたびに測定値が下がって見える**ので、⚠⚠ **「仕様から外れてきた」と誤診し、直す必要のないものを直しにいく。**

✅ **語彙を足し、`# +2026-09-05` の印を付けて 2 段に分けた**（`tools/morning_profile.py`）。⚠ **旧 237 件も新しいリストで測り直してある:**

| 話題 | ⚠ 旧リスト | ✅ 新リスト |
| --- | --- | --- |
| **食べ物・飲み物** | 91（38.4%） | **93（39.2%）** |
| **天気・季節** | 80（33.8%） | **80（33.8%）** |
| **歌・仕事** | 54（22.8%） | **54（22.8%）** |
| どれにも当たらない | 7 件 | **6 件** |

⚠⚠ **旧 237 件はほとんど動かない**（**当然で、リストはこの 237 件から作った**）。🔴 **動くのは新しい原稿のほうだけ** — **だから「リストを足すと過去の数字が変わる」ことを恐れて足さない、という判断にはならなかった。**

⚠ **足す前と後の数字を直接比べない。**⚠⚠ **比べるときは、両方を同じリストで測り直す。**

#### ダジャレは 16 件（6.8%）・型は 3 つ

⚠⚠ **機械では拾えない**ので 237 件を読んで手で拾った（id は `morning_profile.py` の `PUNS`）。

| 型 | 件数 | 例 |
| --- | --- | --- |
| **同音反復** | **10** | イカ / イカしてる・ナン / なんじゃこりゃ・海鮮ドン / ドンまい・ドリアン / どーりやん |
| **「なんちゃって」で閉じる** | 3 | 梨はナシよぅ〜。**なんちゃって。**／コウヨウまた見にいコ〜ヨウ！**なんちゃって。** |
| **慣用句のズラし** | 3 | イチゴ屋、お主もワルよのう（越後屋）／甘いものに目がない → 目が付いてたらビックリだ |

⚠ **元日の原稿が「歌とダジャレと愛をお届けし続けます」と自己申告している**くらいの構成要素だが、🔴 **実測の比率は 15 件に 1 件。**⚠⚠ **毎回入れると別人になる** — **入れない日のほうが圧倒的に多い。**

#### 季節指定は 3 分の 1（82 / 237 件）

- 月ごとの件数: **1月 23 / 2月 24 / 3月 15 / 4月 10 / 🔴 5月 1 / 6月 18 / 7月 15 / 8月 22 / 9月 9 / 10月 11 / 11月 15 / 12月 25**

🔴 **5 月がほぼ空**（1 件）。⚠⚠ **旧原稿を通年で回すと 5 月だけ季節ものが出ない。**⚠ **#17 で書き足すならまず 5 月**（次いで 9 月・4 月）。

#### 取捨選択の軸（#60 の 3 つ）

| 軸 | 実測 | 判断 |
| --- | --- | --- |
| **古びたもの** | **10 件** — 原付の免許 / 新しいスマホ ×2 / DVD レコーダー / ダッフィーくん / ポンデライオン / 食べるラー油 / 来年の手帳 / カラオケの配信 / コンビニの半熟カステラ | ⚠ **捨ててよい**（時事と固有名詞に縛られている） |
| ✅ **学生設定** | **5 件** — 教室の窓 / 通学路の桜 / 学校・試験続き / 学校の友達 / 宿題 | ✅ **残す**（2026-08-14 決定。真琴は本編で中学生） |
| **歌手の日常** | **54 件**（話題「歌・仕事」） | ⚠ **残す価値が高い**（「歌が主役」という筋に合う） |

⚠⚠ **実際に捨てるのは 10 件だけ。**🔴 **「原資が尽きている」の中身は「古い」ではなく「237 件では回らない」**（1 日 1 本なら 8 ヶ月で一周する）。

#### ⚠ #17 が満たすもの（まとめ）

1. **2 行・42 字前後**（1 行目に出来事、2 行目に感想か落ち）
2. 🔴 **敬体が基底**（文単位で 6 割強）。⚠ **崩しは 3 回に 1 回程度で、崩す先は「みんな」「〜だよ」**
3. **一人称は 5 回に 1 回**。⚠ **出すなら自称「真琴」**
4. 🔴 **話題は食べ物 4 割・天気 3 割・歌の仕事 2 割**
5. **ダジャレは 15 件に 1 件**（3 型）。⚠ **「〜な真琴です」の署名オチも同じ頻度**
6. ⚠ **季節ものは 3 分の 1。**🔴 **5 月を埋める**

#### `feature` 列はキーワード機能の名残

`feature` は 388 件中 109 件に入っている（`一般` 59 / `人名` 39 / `地域` 11）。`keyword` / `past_keyword` テーブルが 0 件であることと合わせて、**未実装だった「キーワード学習」の作りかけ**とみられる。絞り込んだ 4 機能には不要。

### 旧実装の好感度モデル（`account.favorability` + `emotion=bad`）— 引き継がない

旧 MAKOTO はアカウントごとに好感度を持ち、**簡単な感情モデル**で応答を切り替えていた。

- **挨拶などで上がり、卑語で下がる**
- `account(id, acct, favorability, nickname)` の `favorability` がその値。実データは 393 件で **-17 〜 7,416** に分布し、0 が 87 件・負の値は 18 件
- **しきい値を下回ると `emotion='bad'` の台詞だけを返すようになる**。`bad` の 25 件は**本編で真琴が怒っている台詞**（キュアソード 9 / 剣崎真琴 16。うち 9 件は `exclude_respond` が立っていて投稿専用）

つまり `emotion=bad` は「卑語を投げてくる相手に怒って返す」ための区分であって、未使用の列ではない。

**makoto2 では引き継がない**（2026-07-30 に確認）。ただし**卑語への応答をどうするかという課題自体は残る**ので、チャットボット（#18）では別の形で扱う。LLM 方式ではプロンプト側の制約として書くほうが素直で、好感度という状態をアカウント単位で永続化する必要は薄い。

### `fairy`（62 件）— 語尾の辞書

妖精名・`human_name`（変身する人間の名前）・`suffix`（語尾）を持つ。`アロマ`→`ロマ`、`キャンディ`→`クル`、`キラリン`→`キラ星 シエル`/`キラ` など。

**makoto2 では使わない**（2026-07-31 決定）。**語尾を管理するという要件自体が無くなった**ため。旧実装は妖精の語尾を辞書で持って文字列を加工していたが、makoto2 は台詞コーパスを LLM の素材として使う方式なので、語尾を機械的に付け替える処理が要らない。

なお、仮に必要になっても **makoto2 に抱え込まない。**妖精はプリキュア作品の情報なので、正本は [cure-api](https://github.com/pooza/cure-api) 側（`series` と同じ理屈 → [CLAUDE.md](CLAUDE.md)）。

### 使い方を変える

⚠ **旧実装は台詞をそのまま返していた**とみられ、文脈を無視して台詞を貼った投稿が実際にあった。これが「キャラのぶれ」の正体である可能性が高い。

**makoto2 では LLM の few-shot / RAG 素材として使う。** 561 件はファインチューニングには少ないが、プロンプト同梱には十分な量。

## 引き継いだ既知ドリフト（nginx `/makoto`）→ ✅ 解消済み（2026-07-31）

nginx の `/makoto` ロケーションは Mastodon フォークの vhost 側にあるため、**キュアスタ！のサーバー移行（lbock → gomander、2026-07-28）でそのまま引き継がれていた**。

- 配置はデルムリン丼とキュアスタ！の 2 台（各サーバーの vhost 設定）。他の 2 台には無かった
- バックエンドの 3011 は誰も listen していないので**実害は 502 のみ**
- `/makoto/sidekiq` の ACL が `/mulukhiya/sidekiq` と食い違ったまま — **退役したホストの旧プレフィクスを許可しており、モロヘイヤ側が持つ現行の許可元が入っていなかった**（具体的な CIDR は chubo2 側を参照）

**この location は踏襲しない。** `/makoto` は旧実装の**管理コンソール**で、パスを知っていたのは運用者ひとりだった。makoto2 に引き継ぐべき利用者も用途も無く、残しても 502 と ACL の食い違いを抱え続けるだけだった。

✅ **2026-07-31 に 2 台とも削除・反映済み**（`pooza/chubo2#99` クローズ）。`location ^~ /makoto/sidekiq` と `location ^~ /makoto` の計 9 行を削除し、`nginx -t` 通過後に reload。**両ホストとも 502 → 404 になったことを確認済み。**

**makoto2 では管理コンソールそのものを作らない。** 運用操作は CLI で行う（→ [CLAUDE.md](CLAUDE.md) の「管理操作は CLI で行う」）。旧実装で Issue が立っていた「WebUI からの辞書更新」も、WebUI ではなく CLI として作る。
